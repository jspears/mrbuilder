{
  "version": "{{version}}",
  "npmClient": "{{npmClient}}",
  "useWorkspaces": true,
  "command": {
    "publish": {
      {{#if gitRemote}}
        "gitRemote": {{gitRemote}}
      {{/if}}
    }
  }
}
